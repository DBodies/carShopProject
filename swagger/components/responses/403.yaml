description: Unauthorized errors
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 403
        message:
          type: string
          example: Forbidden error
        data:
          type: object
