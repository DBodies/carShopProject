tags:
  - Auth
summary: Login or Sign up with google
operationId: SignUp
description: "Login or Sign up with google"
security: []
parameters: []
responses:
  "200":
    description: Login or Sign up with google
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: "Successfully logged in via Google OAuth!"
            data:
              type: object
              example: { accessToken: "Session accessToken" }
